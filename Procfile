# this is fitrst line a alternative if you want to cluster unicorns under nginx
# if you do this you'll also need to 
# web: bin/start-nginx bundle exec unicorn -c config/unicorn.rb

web: bundle exec unicorn -c ./config/unicorn.rb